/* ================================================================
   Stage 7 — Policy Review: security + tenancy + compliance (STUB)
   ================================================================ */

export async function handlePolicyReview(_caseData) {
  return {
    advance: true,
    needsHuman: false,
    error: null,
    summary: 'Policy review auto-passed (stub)',
  };
}

[build]
buildCommand = "npm ci --workspace=apps/api"

[deploy]
startCommand = "node apps/api/src/api/server.js"
healthcheckPath = "/api/health/ping"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
